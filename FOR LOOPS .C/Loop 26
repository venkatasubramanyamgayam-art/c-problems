#include <stdio.h>

int main() {
    int n, count = 0;
 printf("Enter a number: ");
    scanf("%d", &n);
for ( ;n != 0; n = n / 10) 
        count++;
    printf("Number of digits = %d\n", count);

    return 0;
}
