
   #include <stdio.h>
eint main() {
    int N, i , sum =0;
 printf("Enter the value of N: ");
    scanf("%d", &N);
for (i = 1; i <= N; i++) 
        sum =sum+i;
        printf("Sum of first %d natural numbers = %d", sum);
    

    return 0;
}

    
