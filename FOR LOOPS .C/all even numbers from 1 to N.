
   #include <stdio.h>

int main() {
    int N, i ;
 printf("Enter the value of N: ");
    scanf("%d", &N);
    printf("even numbers 1 %d\n",N);
for (i = 1; i<= N; i++) 
       if(i%2==0)
        printf("%d", i);
    

    return 0;
}

    
