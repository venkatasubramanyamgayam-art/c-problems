#include <stdio.h>

int main() {
    int N;
    int data;
    int total_data = 0;
    int high_usage_days = 0;
 scanf("%d", &N);
 for(int i = 0; i < N; i++) {
scanf("%d", &data);
total_data += data;
if(data > 5) {
  high_usage_days++;
 }
}
printf("total data: %d\n", total_data);
printf("high usage Days: %d\n", high_usage_days);

    return 0;
}
