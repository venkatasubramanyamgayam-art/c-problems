#include <stdio.h>
    int main() {
    int a = 10, b = 20, c=30;
printf("enter the numbers\n");

    if (a >= b&&a<=c)
        printf("a is greater than b and c ");
    else 
    printf("a lessthan b and c");

    return 0;
}
