#include <stdio.h>
int main() {
    
    int y;
    printf("enter the number ");
    scanf("%d",&y);
    if (y%5==0)
    printf("divisible by 5");

    else 
    printf("does not divible by 5");

    return 0;
}
