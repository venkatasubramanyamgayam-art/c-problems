#include <stdio.h>
int main() {
    
    char y;
    printf("enter the numbers ");
    scanf("%c",&y);
    if ((y>'A')&&(y<'Z'))
    printf("alphabet");

else if((y>0)&&(y<100))  
  printf("digit");
  else 
  printf("special character");

    return 0;
}
