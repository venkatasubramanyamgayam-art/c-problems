include<stdio.h>
int main(){
int n,i=0;
int hours[100];
int inactive =0;
scanf("%d",&n);
while(i<n)
{
scanf("%d",&hours[i]);
i++;
}
i=0;
while(i<n)
{
if(hours[i]==0)
{
i++;
}
printf("inactive weeks:%d\n",inactive);
if(inactive>=3)
printf("Risk status:high);
else 
printf(risk status :low);
return 0;
}
