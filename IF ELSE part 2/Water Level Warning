#include <stdio.h>
int main() {
    int level ;
    printf("enter the water  level percentage ");
    scanf("%d",&level);
    if(level>=95)
    printf("Overflow Warning");
    else 
    printf(" Safe Level ");
    return 0;
}
