#include <stdio.h>
int main() {
    int marks ;
    printf("enter the marks");
    scanf("%d",&marks);
    if(marks>=85)
    printf(" eligible");
    else 
    printf(" not eligible ");
    return 0;
}
