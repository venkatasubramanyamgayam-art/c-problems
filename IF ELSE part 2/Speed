#include <stdio.h>
int main() {
    int num ;
    printf("enter the speed");
    scanf("%d",&num);
    if((num<=3))
    printf("login allowed ");
    else 
    printf(" account locked");
    return 0;
}
