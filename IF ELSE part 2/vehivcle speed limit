#include <stdio.h>
int main() {
  cle 
    int speed ;
    printf("enter the speed");
    scanf("%d",&speed);
    if((speed>=90))
    printf("Speed Limit Exceeded ");
    else 
    printf(" Within Speed Limit");
    return 0;
}
