#include <stdio.h>
int main() {
    int age ;
    printf("enter the age");
    scanf("%d",&age);
    if((age>=18)&&(age<=70))
    printf("Eligible apply license ");
    else 
    printf(" not Eligible for license");
    return 0;
}
