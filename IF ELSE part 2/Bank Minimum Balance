#include <stdio.h>
int main() {
    int money ;
    printf("enter the balance");
    scanf("%d",&money);
    if(money>=1000)
    printf(" Sufficient Balance");
    else 
    printf(" Low Balance ");
    return 0;
}
